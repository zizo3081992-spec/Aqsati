import type { Client, Installment } from './types';

// This file is now for reference or seeding, but not used by the app directly
// as data is now fetched from Firestore.
export const initialClients: Client[] = [];

export const initialInstallments: Installment[] = [];
